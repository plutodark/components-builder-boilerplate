import styled from 'styled-components'
import { remCalc } from 'src/utils'
const Container = styled.div`
  font-size: ${remCalc(20)};
`
const Testing2 = () => (
  <Container>Testing2</Container>
)

export default Testing2